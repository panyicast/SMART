# 设计变轨策略连续推力参数优化算法

本文档固定当前连续推力变轨参数优化方案，用于从脉冲规划结果生成 5 次连续推力点火参数，并微调前 4 次远地点点火开始时间，使第 5 次点火熄火点经度到达目标经度。

## 1. 适用范围

- 适用于当前 SMART 设计变轨策略页面的超同步转移连续推力参数优化。
- 默认使用 STK 11.6 兼容任务设定和项目配置。
- 轨道动力学采用地球二体引力加 J2 摄动。
- 时间显示和表格输出使用北京时间（UTC+8）。
- 本方案以脉冲规划结果作为事件序列、初始点火时长和初始偏航角来源。

## 2. 输入数据

### 2.1 基础配置

从 `config/design_maneuver_strategy.json` 读取：

- 初始轨道根数与初始质量。
- 目标同步轨道半长轴。
- 目标倾角 `6 deg`。
- 发动机推力、比冲、稳定段参数。
- 每次脉冲规划点火时长、点火位置、偏航角、控后近地点目标。

### 2.2 固定事件序列

采用当前脉冲规划 q 序列：

```text
q = 3, 3, 2, 0
```

对应连续推力事件：

| 变轨 | 事件 |
|---|---|
| MV1 | 初始脉冲规划第 1 次远地点附近 |
| MV2 | MV1 控后轨道推进 3 圈到远地点 |
| MV3 | MV2 控后轨道推进 3 圈到远地点 |
| MV4 | MV3 控后轨道推进 2 圈到远地点 |
| MV5 | MV4 控后轨道推进半圈到近地点 |

## 3. 动力学模型

### 3.1 轨道推进

点火外弧段使用平均根数传播，包含 J2 摄动：

```text
RAAN_dot = -1.5 * n * J2 * (Re / p)^2 * cos(i)
argp_dot = 0.75 * n * J2 * (Re / p)^2 * (5 cos(i)^2 - 1)
M_dot    = n + 0.75 * n * J2 * (Re / p)^2 * sqrt(1 - e^2) * (3 cos(i)^2 - 1)
```

其中：

```text
p = a * (1 - e^2)
n = sqrt(mu / a^3)
```

### 3.2 点火积分

点火段使用 RK4 数值积分。状态量：

```text
[rx, ry, rz, vx, vy, vz, mass]
```

加速度模型：

```text
a_total = a_two_body + a_J2 + a_thrust
```

推力方向为当地水平坐标系下的常值偏航角方向：

```text
direction = cos(yaw) * east - sin(yaw) * north
```

质量流率：

```text
mdot = -F / (Isp * g0)
```

点火分为两段：

1. 稳定段：使用稳定发动机推力与比冲。
2. 轨控段：使用轨控发动机推力与修正后的有效比冲。

## 4. 优化变量

### 4.1 MV1-MV4 远地点变轨

第一轮整体优化变量为 4 个远地点偏航角：

```text
yaw_A = [yaw1, yaw2, yaw3, yaw4]
```

起始时刻初值：

```text
T_start_i = T_event_i - 0.5 * T_pulse_duration_i
```

第二轮微调用 4 个远地点点火开始时间偏移：

```text
dt_A = [dt1, dt2, dt3, dt4]
```

最终固定值：

```text
yaw_A = [5.469752, 10.074242, 15.480019, 14.590538] deg
dt_A  = [-0.009883, 0.202783, -0.481636, 0.597745] min
```

### 4.2 MV5 近地点变轨

MV5 固定为近地点附近面内减速：

```text
yaw5 = -178.537754 deg
```

MV5 开始时间：

```text
T_start_5 = T_perigee_5 - 14.391979 min
```

MV5 熄火目标：

```text
a_cutoff = 42164.2 km
```

## 5. 约束

### 5.1 MV1-MV3

每次远地点变轨熄火时，控后近地点高度达到对应脉冲规划目标：

| 变轨 | 控后近地点高度目标 |
|---|---:|
| MV1 | `3933.000 km` |
| MV2 | `8544.211 km` |
| MV3 | `17422.255 km` |

### 5.2 MV4

MV4 有两个硬约束：

```text
控后近地点高度 = 同步轨道高度
控后倾角 ≈ 6 deg
```

当前固定结果：

```text
MV4 控后倾角 = 6.003562 deg
```

### 5.3 MV5

MV5 熄火目标：

```text
控后半长轴 = 42164.2 km
```

当前固定结果：

```text
控后半长轴 = 42164.199921 km
熄火点经度 = 120.000000 degE
```

## 6. 目标函数

### 6.1 前 4 次远地点变轨

先以总推进剂消耗最小为主要目标，同时强制 MV4 倾角接近 `6 deg`：

```text
minimize:
  sum(m_prop_i), i = 1..4

subject to:
  hp_i = hp_target_i
  i_4 = 6 deg
```

实现中使用罚函数或局部搜索：

```text
score = sum(m_prop_i) + W_i * (i4 - 6)^2
```

### 6.2 MV5

MV5 以点火时长尽量短为目标，熄火半长轴为约束：

```text
minimize:
  burn_duration_5

subject to:
  a_5 = 42164.2 km
```

### 6.3 熄火经度微调

固定 MV5 参数不变，只微调 MV1-MV4 点火开始时间：

```text
variables:
  dt1, dt2, dt3, dt4

minimize:
  W_lon * wrap(lon5_cutoff - 120)^2
+ W_a   * (a5 - 42164.2)^2
+ W_e   * max(0, e5 - e_limit)^2
+ W_dt  * sum(dt_i^2)
```

当前微调量：

| 变轨 | 开始点微调/min | 开始点微调/s |
|---|---:|---:|
| MV1 | `-0.009883` | `-0.593` |
| MV2 | `+0.202783` | `+12.167` |
| MV3 | `-0.481636` | `-28.898` |
| MV4 | `+0.597745` | `+35.865` |

微调后：

```text
MV5 熄火经度 = 120.000000 degE
MV5 控后偏心率 = 0.000261955
MV5 控后半长轴 = 42164.199921 km
```

## 7. 算法流程

### 7.1 远地点变轨整体优化

```text
1. 读取脉冲规划结果。
2. 取 q = [3, 3, 2, 0]。
3. 设置 MV1-MV4 的初始点火开始时刻：
   T_start_i = T_event_i - 0.5 * T_pulse_duration_i
4. 以 yaw1..yaw4 为变量，逐次传播并积分点火：
   MV1: 初始轨道 -> 控后 hp1
   MV2: MV1 后 3 圈远地点 -> 控后 hp2
   MV3: MV2 后 3 圈远地点 -> 控后 hp3
   MV4: MV3 后 2 圈远地点 -> 控后 GEO 近地点高度和 i=6 deg
5. 搜索满足 MV4 倾角硬约束的最小推进剂解。
6. 固定 yaw1..yaw4。
```

### 7.2 MV5 近地点点火

```text
1. 从 MV4 控后轨道推进半圈到近地点。
2. 设置 MV5 面内减速偏航角 yaw5 = -178.537754 deg。
3. 设置点火开始点为近地点前 14.391979 min。
4. 数值积分到半长轴 a = 42164.2 km。
5. 输出 MV5 熄火轨道。
```

### 7.3 熄火经度微调

```text
1. 固定所有偏航角和 MV5 点火规则。
2. 计算当前 MV5 熄火经度误差：
   dlon = wrap(lon5_cutoff - 120)
3. 分别扰动 MV1-MV4 开始点，得到敏感度：
   J_i = d(lon5_cutoff) / d(dt_i)
4. 用 Nelder-Mead 或坐标搜索求 dt1..dt4。
5. 重新传播完整 5 次变轨，确认：
   lon5_cutoff ≈ 120 degE
   a5 ≈ 42164.2 km
   e5 保持较小
```

## 8. 当前固定方案结果

### 8.1 事件点

| 变轨 | 事件位置 | 事件航时/min | 事件时间 BJT | 事件经度/degE |
|---|---|---:|---|---:|
| MV1 | 远地点 | 1254.558372 | 2026-05-15 18:03:52 | 73.475631 |
| MV2 | 远地点 | 4015.758812 | 2026-05-17 16:05:04 | 101.424882 |
| MV3 | 远地点 | 7087.493452 | 2026-05-19 19:16:48 | 51.381116 |
| MV4 | 远地点 | 9552.969301 | 2026-05-21 12:22:17 | 153.001293 |
| MV5 | 近地点 | 10393.915527 | 2026-05-22 02:23:13 | 119.977161 |

### 8.2 点火参数

| 变轨 | 位置 | 开始时间 BJT | 熄火时间 BJT | 点火时长/min | 偏航角/deg | 开始经度/degE | 熄火经度/degE | 推进剂/kg | 控后质量/kg |
|---|---|---|---|---:|---:|---:|---:|---:|---:|
| MV1 | 远地点 | 2026-05-15 17:29:34 | 2026-05-15 18:35:46 | 66.202533 | 5.469752 | 79.222506 | 68.719523 | 605.658455 | 5909.341545 |
| MV2 | 远地点 | 2026-05-17 15:37:18 | 2026-05-17 16:33:20 | 56.023876 | 10.074242 | 105.542823 | 97.686152 | 506.826300 | 5402.515245 |
| MV3 | 远地点 | 2026-05-19 18:43:23 | 2026-05-19 19:51:18 | 67.926640 | 15.480019 | 55.750425 | 47.611114 | 622.399093 | 4780.116152 |
| MV4 | 远地点 | 2026-05-21 11:47:49 | 2026-05-21 12:59:00 | 71.193361 | 14.590538 | 156.700446 | 149.989446 | 654.118117 | 4125.998034 |
| MV5 | 近地点 | 2026-05-22 02:08:50 | 2026-05-22 02:34:54 | 26.071913 | -178.537754 | 119.769418 | 120.000000 | 216.000427 | 3909.997608 |

### 8.3 控后轨道参数

| 变轨 | 半长轴/km | 偏心率 | 倾角/deg | 近地点高度/km | 远地点高度/km | 目标说明 |
|---|---:|---:|---:|---:|---:|---|
| MV1 | 31349.200707 | 0.671087722 | 12.989511 | 3933.000007 | 46009.127407 | 控后近地点 3933 km |
| MV2 | 33658.397212 | 0.556653039 | 10.296007 | 8544.211107 | 46016.309317 | 控后近地点 8544.211 km |
| MV3 | 38106.129641 | 0.375418286 | 7.405110 | 17422.254775 | 46033.730507 | 控后近地点 17422.255 km |
| MV4 | 47304.176106 | 0.108657974 | 6.003562 | 35786.063171 | 46066.015041 | 控后近地点 GEO，倾角约 6 deg |
| MV5 | 42164.199921 | 0.000261955 | 6.005785 | 35775.017806 | 35797.108037 | 控后半长轴 42164.2 km，熄火经度 120 degE |

总推进剂消耗：

```text
2605.002392 kg
```

## 9. 实现建议

### 9.1 服务层函数拆分

建议在 `smart.services.design_maneuver_strategy` 中新增独立流程：

```text
optimize_continuous_thrust_chain_parameters()
```

返回：

- 5 次连续推力参数表。
- 每次点火前后轨道状态。
- 每次点火数值积分历史。
- 经度微调诊断信息。

### 9.2 优化步骤实现

建议实现为三个阶段：

```text
Phase 1: optimize_apogee_yaws_for_terminal_inclination()
Phase 2: solve_mv5_min_duration_to_target_a()
Phase 3: tune_apogee_start_offsets_for_mv5_longitude()
```

### 9.3 数值检查

每次优化后必须检查：

- 所有点火时长不超过配置上限。
- MV1-MV4 控后近地点目标满足误差阈值。
- MV4 控后倾角接近 `6 deg`。
- MV5 控后半长轴接近 `42164.2 km`。
- MV5 熄火经度接近 `120 degE`。
- MV5 控后偏心率保持小量。

## 10. 风险与后续

- 当前方案使用常值偏航角，不能代表闭环制导。
- MV5 低推力弧段不是瞬时脉冲，若同时硬约束近地点高度、远地点高度、经度和最短点火时长，可能需要增加闭环指向或末端修正段。
- 经度微调目前只动 MV1-MV4 开始点，若未来目标经度误差较大，应增加 MV5 开始点或 q 序列作为备用变量。
- 当前数值结果依赖项目配置和积分步长；修改发动机、目标轨道、J2 开关或积分步长后需要重新生成。
